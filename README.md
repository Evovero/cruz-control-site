# cruz-control-site

Static site generator (zero dependencies).

- Build: `node build.mjs` (outputs to `dist/`)
- Edit business facts/services/locations in `src/data/site.mjs`
- Blog posts go in `posts/` as Markdown and become pages on build
